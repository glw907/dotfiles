#!/usr/bin/env bash
# Launch the dedicated jrnl-md journaling environment.
# Opens today's journal in a styled Kitty window.

exec kitty \
    --class journal \
    --config ~/.config/kitty/kitty-journal.conf \
    --title Journal \
    -e bash -l -c '$HOME/go/bin/jrnl-md edit'
