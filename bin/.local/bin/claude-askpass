#!/usr/bin/env bash
# SUDO_ASKPASS helper for Claude sessions. Reads the tmpfs cache written by
# claude-sudo-setup; a stale or missing cache means that script must run again.
cat "/dev/shm/claude-sudo-$(id -u)" 2>/dev/null
