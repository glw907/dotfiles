#!/usr/bin/env bash
# SUDO_ASKPASS helper for Claude sessions
# Try age-encrypted file first, fall back to 1Password-cached token
age -d -i "${AGE_KEY_FILE:-$HOME/.config/age/asc-key.txt}" \
    ~/.cache/sudo-password.age 2>/dev/null \
    || cat ~/.cache/claude-sudo-token 2>/dev/null
