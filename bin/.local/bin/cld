#!/usr/bin/env bash
# cld — thin alias for claude (modal pattern retired 2026-03-19)
# Model and session context now handled via settings.json and SessionStart hook.
# Kept as a convenience alias; behavior is identical to running `claude` directly.
exec claude "$@"
